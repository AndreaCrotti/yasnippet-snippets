# -*- mode: snippet -*-
# name: bookinbook
# key: bookinbook
# contributor: Spenser Truex
# --
@bookinbook{ ${title},
author    = {${author}},
title     = {${title}},
chapter   = {${chapter}}${,
pages     = {${pages}}},
publisher = {${publisher}},
year      = {${year}},
volume    = {${volume}}${,
series    = {${series}}}${,
type      = {${type}}}${,
address   = {${address}}}${,
edition   = {${edition}}}${,
month     = {${month}}}${,
note      = {${note}}}
}

$0