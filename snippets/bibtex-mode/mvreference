# -*- mode: snippet -*-
# name: mvereference
# key: mvreference
# contributor: Spenser Truex
# --
@mvreference{ ${title},
editor    = {${editor}},
title     = {${title}},
year      = {${year}}${,
publisher = {${publisher}}}${,
volumes   = {${volumes}}}${,
series    = {${series}}}${,
type      = {${type}}}${,
chapter   = {${chapter}}}${,
pages     = {${pages}}}${,
address   = {${address}}}${,
edition   = {${edition}}}${,
month     = {${month}}}${,
note      = {${note}}}
}

$0