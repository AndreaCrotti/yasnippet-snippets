# -*- mode: snippet -*-
# name: StatelessWidget
# key: slw
# group: flutter
# --
class ${1:Name} extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container($0);
  }
}