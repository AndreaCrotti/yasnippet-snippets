# -*- mode: snippet -*-
# name: google_service_networking_connection
# key: goog_service_networking_connection
# --

resource "google_service_networking_connection" "${1:name}" {
  network                 = "${2:network}"
  service                 = "${3:service}"
  reserved_peering_ranges = ["${4:reserved_peering_ranges}"]
}

