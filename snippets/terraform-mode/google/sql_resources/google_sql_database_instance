# -*- mode: snippet -*-
# name: google_sql_database_instance
# key: goog_sql_database_instance
# --

resource "google_sql_database_instance" "${1:name}" {
  region = "${2:region}"
  settings {
    tier = "${3:tier}"
  }
}

