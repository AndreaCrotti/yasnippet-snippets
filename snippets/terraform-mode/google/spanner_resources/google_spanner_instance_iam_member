# -*- mode: snippet -*-
# name: google_spanner_instance_iam_member
# key: goog_spanner_instance_iam_member
# --

resource "google_spanner_instance_iam_member" "${1:name}" {
  instance  = "${2:instance}"
  role      = "${3:role}"
  member    = "user:${4:user}"
}

