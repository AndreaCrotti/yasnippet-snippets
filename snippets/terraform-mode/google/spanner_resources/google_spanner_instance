# -*- mode: snippet -*-
# name: google_spanner_instance
# key: goog_spanner_instance
# --

resource "google_spanner_instance" "${1:name}" {
  config       = "${2:config}"
  display_name = "${3:display_name}"
}

